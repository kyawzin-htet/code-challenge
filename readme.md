# 99Tech Code Challenge Submission

This repository contains my solutions for the 99Tech code challenge.


## Problem 1

`src/problem1/script.js` includes three implementations of `sum_to_n`:

- Formula-based solution with `O(1)` time complexity
- Iterative loop solution with `O(n)` time complexity
- Recursive solution with `O(n)` time complexity

## Problem 2

`src/problem2` contains a Vite-powered currency swap interface.

More implementation details are available in `src/problem2/readme.md`.

## Problem 3

`src/problem3/readme.md` contains the code review response, including identified issues, inefficiencies, anti-patterns, and a refactored React/TypeScript version.
